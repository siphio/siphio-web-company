# PIV Orchestrator — Environment Variables

# Required: OAuth token from your Anthropic subscription (Claude Pro/Max)
# This uses your existing subscription billing — no separate API credits needed.
# Generate via: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=

# Target project directory (defaults to current working directory)
PIV_PROJECT_DIR=

# Model override (defaults to claude-opus-4-6)
PIV_MODEL=

# Telegram Bot (optional — enables remote control)
# Create a bot via @BotFather on Telegram, get the token
TELEGRAM_BOT_TOKEN=

# Your personal chat ID (get it from @userinfobot)
TELEGRAM_CHAT_ID=

# Project name prefix for messages (defaults to project directory name)
TELEGRAM_PROJECT_PREFIX=

# Multi-instance: When running multiple projects with the same bot token,
# the first instance to start owns the Telegram polling connection.
# Other instances can still SEND notifications.
# Use /status all in Telegram to see all running instances.
# Set unique TELEGRAM_PROJECT_PREFIX per project for clarity.

# Disable instance registry (defaults to enabled)
# PIV_REGISTRY_DISABLED=1

# Runtime artifacts (auto-created by orchestrator — do not edit)
# Log file: .agents/orchestrator.log
# PID file: .agents/orchestrator.pid
# Signal file: .agents/orchestrator.signal
# Global registry: ~/.piv-orchestrator/registry.json
